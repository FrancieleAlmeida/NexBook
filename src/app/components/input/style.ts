import { StyleSheet } from "react-native";

export const styles = StyleSheet.create({
  input: {
    width: "100%",
    height: 52,
    borderRadius: 10,
    borderBottomWidth: 1,
    borderColor: "#000",
    padding: 12,
    fontSize: 16,
    color: "#000",
  }
})